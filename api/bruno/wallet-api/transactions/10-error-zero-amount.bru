meta {
  name: Transfer - Zero Amount (Error)
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/transactions
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "account_id_from": "{{johnAccountId}}",
    "account_id_to": "{{janeAccountId}}",
    "amount": 0
  }
}

tests {
  test("should reject zero amount", function() {
    expect(res.status).to.equal(400);
  });
}

docs {
  Error scenario: Transaction with zero amount should be rejected.
}
