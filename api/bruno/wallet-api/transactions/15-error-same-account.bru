meta {
  name: Transfer - Same Account (Error)
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/transactions
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "account_id_from": "{{johnAccountId}}",
    "account_id_to": "{{johnAccountId}}",
    "amount": 100
  }
}

docs {
  Error scenario: Transaction to the same account.
  This might be allowed or rejected depending on business rules.
}
