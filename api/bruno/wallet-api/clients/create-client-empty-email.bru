meta {
  name: Create Client - Empty Email (Error)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/clients
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Test User",
    "email": ""
  }
}

tests {
  test("should return error for empty email", function() {
    expect(res.status).to.equal(500);
  });
}
