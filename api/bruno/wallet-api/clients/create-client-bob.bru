meta {
  name: Create Client - Bob Wilson
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/clients
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Bob Wilson",
    "email": "bob@example.com"
  }
}

script:post-response {
  if (res.status === 200 || res.status === 201) {
    bru.setVar("bobClientId", res.body.ID);
  }
}
